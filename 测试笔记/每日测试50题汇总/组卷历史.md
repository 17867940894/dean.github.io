## 2026-08-10
1. 在软件生命周期瀑布模型中，设计阶段包含概要设计与详细设计，其中详细设计阶段的主要工作是？
2. 以下关于黑盒测试优点的描述，正确的是？
3. 系统测试主要能发现的缺陷类型通常是？
4. 测试的四个基本活动中，“测试计划”阶段的主要工作是？
5. ISO 25010 质量模型中，描述“功能是否完备、正确、恰当”的特性是？
6. ISO 25010 质量模型中，“兼容性”特性主要包含以下哪两个子特性？
7. 测试用例 8 要素中，“预期结果”描述的是？
8. 缺陷严重程度为“轻微”的缺陷，通常指？
9. 缺陷管理流程中，缺陷回归验证不通过时，缺陷状态通常会被置为？
10. 软件测试七大原则中，“测试活动依赖上下文”的含义是？
11. 以下哪项属于验证（Verification）的典型活动？
12. 在 Linux 中，用于切换当前登录用户（如切换到 root 超级用户）的命令是？
13. 基于经验与历史缺陷推测易错点，从而补充设计测试用例的方法称为？
14. 判定表法（决策表法）主要适用于哪种场景？
15. 对于 URL `https://www.example.com:8443/api/user?id=100`，其中“路径”部分是？
16. 以下哪种场景最适合引入自动化测试？
17. 兼容性测试主要验证的内容是？
18. 关于压力测试（Stress Testing），以下描述正确的是？
19. Selenium 中，使用 find_element 定位元素但页面上不存在该元素时，会？
20. 某元素 class=“s_ipt bg”（含两个类名），直接使用 By.CLASS_NAME 定位会报错，正确的做法是？
21. 在 XPath 中，用于定位文本包含“新闻”二字的 <a> 链接的写法是？
22. HTTP 状态码 201 Created 表示？
23. HTTP 状态码 302 Found 表示？
24. 在 Linux 中，用于在文件中搜索包含指定关键字的行并输出结果的命令是？
25. 在 Android 测试中，用于查看 APK 安装包详细信息的命令行工具是？
26. 以下哪些属于 Selenium 的元素定位方式？
27. 关于 TCP 与 UDP 协议的区别，以下描述正确的有？
28. 以下哪些属于 ADB 的常用命令？
29. 以下哪些属于 SQL 语言的分类？
30. 以下哪些属于常见的软件测试模型？
31. 关于 HTTP 状态码，以下对应正确的有？
32. 以下哪些属于 Selenium 的等待方式？
33. 在 Python 中，以下哪些属于字符串（str）的常用方法？
34. 以下哪些属于移动 App 的异常测试场景？
35. 以下哪些属于 JMeter 脚本中的常用元件？
36. HTTP 协议默认使用的端口号是 ______。
37. TCP 协议在建立连接时，需要进行 ______ 次握手。
38. 在 Linux 中，用于显示当前所在工作目录的命令是 ______。
39. 在 Linux 中，用于查看当前系统进程快照的常用命令是 ______。
40. Selenium 中，find_elements 方法在页面上找不到任何匹配元素时，返回 ______。
41. 在使用 adb logcat 查看日志时，用于先清空历史日志、只输出新日志的参数是 ______。
42. 在 SQL 语言中，SELECT 语句属于 ______ 语言（填写 DDL/DML/DQL/DCL 之一）。
43. Python 中，元素唯一且无序、支持数学集合运算（如并集、交集）的数据类型是 ______。
44. HTTPS 协议默认使用的端口号是 ______。
45. 边界值分析法中，针对等价类边界设计的三个典型点是上点、离点和 ______。
46. 请简述 HTTP 与 HTTPS 协议的主要区别（至少三点），并说明 HTTPS 在传输层使用的加密机制。
47. 请简述 Selenium 元素定位的八大方式，并说明生产环境中选择定位方式的基本原则（推荐优先级）。
48. 请简述 JMeter 性能测试脚本中常用元件（线程组、取样器、断言、监听器、后置处理器）的作用。
49. 请简述 Android 中 monkey 稳定性测试的基本命令格式，并说明 -p、--throttle、-s、-vvv 及 COUNT 各参数的含义。
50. 请简述 SQL 语言的四大分类（DDL/DML/DQL/DCL）各自的作用，并分别写出一个典型关键字。

## 2026-08-11
1. 软件缺陷的判定标准中，以下哪一项不属于缺陷？
2. 在软件测试流程中，需求分析（评审）阶段的参与人员通常不包括以下哪类角色？
3. 开发人员修复缺陷并提交新版本后，测试人员为确认该缺陷已被修复且未引入新问题，通常执行哪种测试？
4. 关于判定表法的使用，以下描述正确的是？
5. 边界值分析法中，"开内闭外"优化策略的含义是？
6. 按照国内软件测试的惯例，单元测试的主要执行人员通常是？
7. 与 HTTP/1.0 相比，HTTP/1.1 的核心改进是？
8. HTTP 状态码 503 Service Unavailable 表示？
9. 关于 OSI 参考模型"表示层"的功能，以下描述正确的是？
10. IPv4 地址中，属于 C 类地址范围的是？
11. 视频直播、在线游戏等场景通常使用 UDP 而不是 TCP，主要原因是？
12. 在 Linux 中，用于查看命令或配置文件帮助信息的通用命令是？
13. Linux 中，命令 tail -f 的主要用途是？
14. 在 Linux 中，ls -l 输出的第一列 drwxr-xr-x 中，第一个字符 d 表示？
15. Linux 中，tar -zcvf houma.tar.gz 选项中 z 的含义是？
16. Linux 中，用于强制终止指定进程的命令是？
17. Selenium 中，find_element(By.LINK_TEXT, '新闻') 对链接文本的要求是？
18. Selenium 显式等待中，WebDriverWait(driver, 10).until(条件) 里 locator 参数的写法要求是？
19. 在 Selenium 中处理 iframe 内的元素时，第一步需要执行的操作是？
20. 性能测试中，"90% 响应时间"的含义是？
21. JMeter 线程组中，Ramp-Up 时间（启动时间）表示？
22. Android 中，adb -s 设备号 shell 命令中 -s 参数的作用是？
23. 在 HTTP 请求方法中，GET 与 POST 的主要区别是？
24. 在 HTML 中，用于引入外部 CSS 样式文件的标签是？
25. Python 中，用于返回序列长度（元素个数）的内置函数是？
26. 缺陷报告提交时，以下哪些属于必须填写或描述的信息？（多选）
27. 以下哪些属于软件缺陷产生的常见原因？（多选）
28. 关于 HTTP 请求，以下描述正确的有？（多选）
29. 以下哪些属于 Linux 的压缩和解压缩类命令？（多选）
30. 关于 TCP 三次握手建立连接的过程，以下描述正确的有？（多选）
31. Selenium 显式等待中，以下哪些属于 expected_conditions 提供的常用条件？（多选）
32. JMeter 中，以下哪些属于常用的控制器类型？（多选）
33. Python 中，以下哪些属于字典（dict）的常用操作方法？（多选）
34. 关于 Android 系统，以下描述正确的有？（多选）
35. 以下哪些属于性能测试的常见分类？（多选）
36. 在 Linux 中，将命令输出结果覆盖写入文件（而非追加）使用的重定向符号是 ______。
37. HTTP 协议工作在 OSI 参考模型的 ______ 层。
38. 在 Linux 中，查看已经执行过的历史命令的命令是 ______。
39. Selenium 显式等待中，WebDriverWait 的 until 方法用于等待条件成立，与之对应、等待条件不再成立时继续执行的方法是 ______。
40. 性能测试中，成功率通常要求达到 ______ 以上（填写数值百分比）。
41. 在 VIM 编辑器中，保存文件并退出的命令是 ______。
42. HTTP/2 相比 HTTP/1.1 在连接复用上的核心改进是 ______，可有效解决队头阻塞问题。
43. 在 Linux 中，用于查看文件系统磁盘空间占用情况的命令是 ______。
44. 在 HTML 中，用于引入外部 JavaScript 文件的标签是 <______>。
45. 在 VIM 编辑器中，从编辑模式回到一般模式需要按下 ______ 键。
46. 请简述软件测试的完整流程（从需求分析到测试报告），并说明每个阶段的主要工作。
47. 请简述 GET 与 POST 请求方法的区别（至少三点），并说明各自适用的典型场景。
48. 请简述 HTTP/1.0 与 HTTP/1.1 的主要区别，并说明 HTTP/1.1 默认使用长连接带来的优势。
49. 请简述 Linux 中 crontab 定时任务的基本格式（五个时间字段各自的含义），并举例说明如何设置"每天凌晨 3 点执行某命令"。
50. 请简述 Linux 中 grep 命令的常用参数及其作用（如 -i、-v、-n、-c、-r），并说明管道符 "|" 的作用。
